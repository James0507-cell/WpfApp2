<Window x:Class="WpfApp2.ShortCheck"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="ShortCheck" Height="744" Width="852">
    <Border BorderThickness="1" CornerRadius="1,1,1,1" Margin="0,0,0,-6" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform/>
                <SkewTransform AngleX="0"/>
                <RotateTransform/>
                <TranslateTransform X="0"/>
            </TransformGroup>
        </Border.RenderTransform>
        <Border BorderBrush="Black" BorderThickness="1" CornerRadius="1,1,1,1" Margin="-1,-1,-1,-1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="113*"/>
                    <RowDefinition Height="317*"/>
                    <RowDefinition Height="302*"/>
                </Grid.RowDefinitions>
                <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,0,56">
                    <Grid Margin="0,0,0,-1">
                        <Image Margin="18,21,777,19" Source="/sdasd.jpg"/>
                        <Label Content="Short Check" HorizontalAlignment="Left" Margin="83,0,0,0" VerticalAlignment="Center" Width="98" Height="33" Foreground="#FF00104D" FontWeight="SemiBold" FontSize="14"/>
                    </Grid>
                </Border>
                <Grid Margin="0,113,0,0" Grid.RowSpan="2">
                    <Border BorderBrush="Black" BorderThickness="1" Margin="172,30,180,31" CornerRadius="15,15,15,15">
                        <Grid>
                            <Label Content="Record Your Measurements" HorizontalAlignment="Left" Margin="24,19,0,0" VerticalAlignment="Top" Width="155"/>
                            <Label Content="Enter your current height and weight to track your health metrics." HorizontalAlignment="Left" Margin="24,32,0,0" VerticalAlignment="Top" Width="368" Foreground="#FF797979"/>
                            <Label Content="Height (cm)" HorizontalAlignment="Left" Margin="24,75,0,0" VerticalAlignment="Top" Width="99" FontWeight="SemiBold"/>
                            <TextBox HorizontalAlignment="Left" Margin="26,101,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="300">
                                <TextBox.Template>
                                    <ControlTemplate TargetType="{x:Type TextBox}">
                                        <Border x:Name="Border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="5"
                    Padding="{TemplateBinding Padding}">
                                            <ScrollViewer x:Name="PART_ContentHost"
                              Focusable="False"
                              HorizontalScrollBarVisibility="Hidden"
                              VerticalScrollBarVisibility="Hidden"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                            <Label Content="Weight (kg)" HorizontalAlignment="Left" Margin="24,140,0,0" VerticalAlignment="Top" Width="99" FontWeight="SemiBold"/>
                            <TextBox HorizontalAlignment="Left" Margin="26,166,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="300">
                                <TextBox.Template>
                                    <ControlTemplate TargetType="{x:Type TextBox}">
                                        <Border x:Name="Border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="5"
                                            Padding="{TemplateBinding Padding}">
                                            <ScrollViewer x:Name="PART_ContentHost"
                                                Focusable="False"
                                                HorizontalScrollBarVisibility="Hidden"
                                                VerticalScrollBarVisibility="Hidden"/>
                                        </Border>
                                    </ControlTemplate>
                                </TextBox.Template>
                            </TextBox>
                            <Button Content="" HorizontalAlignment="Center" Margin="0,221,0,0" VerticalAlignment="Top" Width="290">
                                <Button.Background>
                                    <ImageBrush ImageSource="/213sda.jpg"/>
                                </Button.Background>
                            </Button>
                        </Grid>
                    </Border>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="172,336,180,-275" CornerRadius="15,15,15,15">
                        <Grid/>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Border>
</Window>
