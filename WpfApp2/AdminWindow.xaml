<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" x:Class="WpfApp2.AdminWindow"
        mc:Ignorable="d"
        Title="AdminWindow" Height="744" Width="852" Background="{x:Null}" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="CustomComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Background" Value="#FFE5E5E5"/>
            <Setter Property="BorderBrush" Value="#1A00104D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <!-- Outer Border -->
                            <Border x:Name="Border"
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="35" />
                                        <!-- Right arrow area -->
                                    </Grid.ColumnDefinitions>

                                    <!-- Displayed Text -->
                                    <ContentPresenter Grid.Column="0"
                                                  Margin="6,2,6,2"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  RecognizesAccessKey="True"/>

                                    <!-- Drop Down Button on Right -->
                                    <ToggleButton x:Name="ToggleButton"
                                              Grid.Column="1"
                                              Focusable="False"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press"
                                              Background="Transparent"
                                              BorderThickness="0">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                <Border CornerRadius="0,5,5,0"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    HorizontalAlignment="Stretch"
                                                    VerticalAlignment="Stretch">
                                                    <Path x:Name="Arrow"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="Black"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Width="10"
                                                      Height="6"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Arrow" Property="Fill" Value="#FF014B8E"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>

                            <!-- Popup DropDown -->
                            <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                                <Border Background="White"
                                    BorderBrush="Gray"
                                    BorderThickness="1"
                                    CornerRadius="5">
                                    <ScrollViewer Margin="4,6,4,6"
                                              SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <!-- Visual States -->
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FFF0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FF014B8E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#FFE5E5E5"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FFAAAAAA"/>
                                <Setter Property="Foreground" Value="#FF888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AppointmentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#1C3E61"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E6E6E6"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SelectedAppointmentButtonStyle" TargetType="Button" BasedOn="{StaticResource AppointmentButtonStyle}">
            <Setter Property="Background" Value="#E53935"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#E53935"/>
        </Style>

        <Style x:Key="BookedAppointmentStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#A0A0A0"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,15"/>
            <Setter Property="IsEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
        BorderBrush="{TemplateBinding BorderBrush}"
        BorderThickness="{TemplateBinding BorderThickness}"
        CornerRadius="5">

                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                                <ContentPresenter/>

                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TabControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid ClipToBounds="True" SnapsToDevicePixels="True" KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" HorizontalAlignment="Center">
                                <TabPanel x:Name="HeaderPanel" 
                                      IsItemsHost="True" 
                                      Background="Transparent" 
                                      Margin="{TemplateBinding Padding}"
                                      KeyboardNavigation.TabIndex="1" 
                                      ZIndex="1"/>
                            </Grid>

                            <Border x:Name="ContentPanel" 
                                Grid.Row="1" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                KeyboardNavigation.TabIndex="2" 
                                KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter x:Name="PART_SelectedContentHost" 
                                              ContentSource="SelectedContent" 
                                              Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="TabBorder"
                            Background="#F0F0F0"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            CornerRadius="15">
                            <ContentPresenter x:Name="ContentSite"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          ContentSource="Header"
                                          Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Selected tab -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="MidnightBlue"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="MidnightBlue"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>

                            <!-- Hover on inactive -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#E0E0E0"/>
                            </Trigger>

                            <!-- Hover on active tab -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="TabBorder" Property="Background" Value="#191970"/>
                            </MultiTrigger>

                            <!-- Inactive tab -->
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="TabBorder" Property="Background" Value="#FFCBCBCB"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>


    <Border BorderThickness="1" Background="#FFF9F9F9" CornerRadius="20,20,20,20">
        <Grid Margin="-1,-1,-1,-9">
            <Grid.RowDefinitions>
                <RowDefinition Height="57"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border BorderBrush="#FFB1B1B1" BorderThickness="0,0,0,1" Height="50"/>
            <Label Content="Welcome Back!, Admin" HorizontalAlignment="Left" Margin="67,28,0,0" VerticalAlignment="Top" FontSize="8" Foreground="#FF797979" Height="22" Width="91"/>
            <Image HorizontalAlignment="Left" Height="56" VerticalAlignment="Center" Width="55" Source="/Logo-Final_noname_1.png" RenderTransformOrigin="1.025,-0.28" Margin="12,0,0,0"/>
            <Button Content="Logout" HorizontalAlignment="Right" Margin="0,10,10,10" VerticalAlignment="Center" Height="20" Width="62" Style="{StaticResource AppointmentButtonStyle}" Click="Button_Click" FontSize="8">

            </Button>
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <TabControl Margin="40,36,40,71" BorderBrush="{x:Null}" Background="{x:Null}" SelectionChanged="TabControl_SelectionChanged_2">
                    <TabItem Header="🏠 Overview" Background="{x:Null}" Margin="-104,-1,76,-1" BorderThickness="5,0,0,0" BorderBrush="{x:Null}">
                        <Grid Height="510" Margin="0,0,-1,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="121*"/>
                                <RowDefinition Height="389*"/>
                            </Grid.RowDefinitions>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Border BorderBrush="#1A00104D" BorderThickness="1" Margin="5,0,5,0" CornerRadius="5,5,5,5" Background="White" VerticalAlignment="Center" Height="63">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="2" Opacity="0.5" Direction="270" Color="#CCCCCC"/>
                                    </Border.Effect>
                                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Image HorizontalAlignment="Left" Height="30" VerticalAlignment="Center" Width="36" Source="/times.jpg" Margin="4,0,0,0"/>
                                        <Label Content="Pending Appointments" HorizontalAlignment="Left" Margin="34,0,0,0" VerticalAlignment="Top" Width="132"/>
                                        <Label x:Name="lblPending" Content="0" Margin="85,20,0,0" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="15" RenderTransformOrigin="0.069,0.537" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" HorizontalAlignment="Left"/>
                                    </Grid>
                                </Border>
                                <Border BorderBrush="#1A00104D" BorderThickness="1" Grid.Column="1" CornerRadius="5,5,5,5" Background="White" VerticalAlignment="Center" Margin="5,0,6,0" Height="63">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="2" Opacity="0.5" Direction="270" Color="#CCCCCC" RenderingBias="Quality"/>
                                    </Border.Effect>
                                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="172">
                                        <Label Content="Medicine Request" HorizontalAlignment="Left" Margin="42,-1,0,0" VerticalAlignment="Top" Width="123"/>
                                        <Label x:Name="lblMedicine" Content="0" HorizontalAlignment="Center" Margin="0,20,0,0" VerticalAlignment="Top" FontSize="15" FontWeight="SemiBold"/>
                                        <Image HorizontalAlignment="Left" Height="34" Margin="6,10,0,0" VerticalAlignment="Top" Width="36" Source="/capsule icon.jpg"/>
                                    </Grid>
                                </Border>
                                <Border BorderBrush="#1A00104D" BorderThickness="1" Grid.Column="2" Margin="4,0,5,0" CornerRadius="5,5,5,5" Background="White" VerticalAlignment="Center" Height="64">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="2" Opacity="0.5" Direction="270" Color="#CCCCCC"/>
                                    </Border.Effect>
                                    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Height="48">
                                        <Image Margin="7,7,134,10" Source="/iconcaustiosn.jpg"/>
                                        <Label Content="Low Stack Items" HorizontalAlignment="Left" Margin="50,-2,0,0" VerticalAlignment="Top" Width="123"/>
                                        <Label x:Name="lblLowStack" Content="0" HorizontalAlignment="Center" Margin="0,19,0,0" VerticalAlignment="Top" FontWeight="SemiBold" FontSize="15" HorizontalContentAlignment="Center"/>
                                    </Grid>
                                </Border>
                                <Border BorderBrush="#1A00104D" BorderThickness="1" Grid.Column="3" Margin="5,0,5,0" CornerRadius="5,5,5,5" Background="White" VerticalAlignment="Center" Height="62">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="2" Opacity="0.5" Direction="270" Color="#CCCCCC"/>
                                    </Border.Effect>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="171">
                                        <Grid Height="50" HorizontalAlignment="Center" Width="170" VerticalAlignment="Center">
                                            <Image HorizontalAlignment="Left" Height="32" Margin="8,6,0,0" VerticalAlignment="Top" Width="41" Source="/persons.jpg"/>
                                            <Label Content="Active Status" HorizontalAlignment="Left" Margin="49,-1,0,0" VerticalAlignment="Top" HorizontalContentAlignment="Center"/>
                                            <Label x:Name="lblActiveStatus" Content="0" HorizontalAlignment="Center" Margin="0,17,0,0" VerticalAlignment="Top" FontSize="15" FontWeight="SemiBold" VerticalContentAlignment="Center"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            <Grid Grid.Row="1" Margin="0,0,0,32">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid Margin="0,0,0,-32">
                                    <Border BorderThickness="1,1,1,1" BorderBrush="#FFD1D1D1" CornerRadius="10,10,10,10" Background="White" SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="HighQuality" Margin="0,0,0,10">

                                        <Grid Margin="0,0,-2,-10">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="70"/>
                                                <RowDefinition Height="312*"/>
                                                <RowDefinition Height="29*"/>
                                            </Grid.RowDefinitions>
                                            <ScrollViewer Grid.Row="1">
                                                <StackPanel x:Name="StackPanelActivities"/>
                                            </ScrollViewer>
                                            <Border Grid.Row="0"
        BorderBrush="#FFA0A0A0"
        BorderThickness="0,0,0,1"
        Margin="0,4,0,0"
        VerticalAlignment="Bottom">
                                                <Grid Height="65" VerticalAlignment="Bottom">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="270"/>
                                                        <ColumnDefinition Width="240" MinWidth="221"/>
                                                        <ColumnDefinition Width="1" MinWidth="180"/>
                                                        <ColumnDefinition Width="1"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="55.8" MinHeight="55.58"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Title -->
                                                    <Label Content="Recent Activities"
               Foreground="#FF00104D"
               FontSize="14"
               FontWeight="DemiBold"
               Padding="5,0,0,0"
               Margin="10,12,0,25"/>

                                                    <!-- Subtitle -->
                                                    <Label Content="Get an overview of the recent activities"
               Grid.Column="0"
               Margin="10,30,0,0"
               HorizontalAlignment="Left"
               VerticalAlignment="Top"
               Foreground="#FFAFAFAF"
               Height="26"
               Width="236"/>

                                                    <!-- Search Box -->
                                                    <Border Grid.Column="1"
                BorderBrush="#1A00104D"
                BorderThickness="1"
                CornerRadius="5"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
                Width="220"
                Height="32"
                Margin="0,24,0,0">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>

                                                            <Image Grid.Column="0"
                       HorizontalAlignment="Center"
                       Height="20"
                       VerticalAlignment="Center"
                       Width="17"
                       Margin="5,0,5,0"
                       Source="/Screenshot 2025-10-21 210426.png"/>

                                                            <TextBox x:Name="txtSearchActivity"
                         Grid.Column="1"
                         HorizontalAlignment="Stretch"
                         TextWrapping="Wrap"
                         VerticalAlignment="Center"
                         BorderBrush="{x:Null}"
                         Height="28"
                         VerticalContentAlignment="Center"
                         Foreground="#FF8F8F8F"
                         GotFocus="txtSearchActivity_GotFocus"
                         LostFocus="txtSearchActivity_LostFocus"
                         TextChanged="txtSearchActivity_TextChanged"
                         Text="Search activity ID..."/>
                                                        </Grid>
                                                    </Border>
                                                    <ComboBox x:Name="cboFilterActivity" Grid.Column="2" HorizontalAlignment="Left" Margin="10,26,0,0" VerticalAlignment="Top" Width="170" Style="{StaticResource CustomComboBoxStyle}" SelectionChanged="ComboBox_SelectionChanged_1" Foreground="Black" Height="30">
                                                        <ComboBoxItem Content="All"/>
                                                        <ComboBoxItem Content="Appointment Approved"/>
                                                        <ComboBoxItem Content="Appointment Rejected"/>
                                                        <ComboBoxItem Content="Medicine Request Approved"/>
                                                        <ComboBoxItem Content="Medicine Request Rejected"/>
                                                        <ComboBoxItem Content="Add New Student"/>
                                                        <ComboBoxItem Content="Update Student Info"/>
                                                        <ComboBoxItem Content="Delete Student Info"/>
                                                        <ComboBoxItem Content="Update Medicine Inventory"/>
                                                        <ComboBoxItem Content="Add New Medicine"/>
                                                    </ComboBox>

                                                </Grid>
                                            </Border>

                                        </Grid>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="💊 Medicine" Margin="89,-2,-114,0" BorderBrush="{x:Null}" Background="{x:Null}">
                        <Grid>
                            <Grid>
                                <Border BorderBrush="#1A00104D" BorderThickness="1" Margin="0,20,0,0" CornerRadius="10,10,10,10" Background="White">
                                    <Grid Margin="-1,-1,-1,1">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="70"/>
                                            <RowDefinition Height="211*"/>
                                        </Grid.RowDefinitions>
                                        <ScrollViewer Grid.Row="1" Margin="10,9,0,0">
                                            <StackPanel x:Name="StackPanelMedicineReuqests" HorizontalAlignment="Stretch"/>
                                        </ScrollViewer>
                                        <Border Grid.Row="0" BorderBrush="#FFA0A0A0" BorderThickness="0,0,0,1" Margin="0,4,0,0">
                                            <Grid Height="65" VerticalAlignment="Bottom">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="270"/>
                                                    <ColumnDefinition Width="240" MinWidth="221"/>
                                                    <ColumnDefinition Width="1" MinWidth="180"/>
                                                    <ColumnDefinition Width="1"/>
                                                </Grid.ColumnDefinitions>

                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="55.8" MinHeight="55.58"/>
                                                </Grid.RowDefinitions>

                                                <!-- Title -->
                                                <Label Content="Medicine Request"
               Foreground="#FF00104D"
               FontSize="14"
               FontWeight="DemiBold"
               Padding="5,0,0,0"
               Margin="10,12,0,25"/>

                                                <!-- Subtitle -->
                                                <Label Content="Manage and review medicine request"
               Grid.Column="0"
               Margin="10,30,0,0"
               HorizontalAlignment="Left"
               VerticalAlignment="Top"
               Foreground="#FFAFAFAF"
               Height="26"
               Width="236"/>

                                                <!-- Search Box -->
                                                <Border Grid.Column="1"
                BorderBrush="#1A00104D"
                BorderThickness="1"
                CornerRadius="5"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
                Width="220"
                Height="32"
                Margin="0,24,0,0">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>

                                                        <Image Grid.Column="0"
                       HorizontalAlignment="Center"
                       Height="20"
                       VerticalAlignment="Center"
                       Width="17"
                       Margin="5,0,5,0"
                       Source="/Screenshot 2025-10-21 210426.png"/>

                                                        <TextBox x:Name="txtSearchMedID"
                         Grid.Column="1"
                         HorizontalAlignment="Stretch"
                         TextWrapping="Wrap"
                         VerticalAlignment="Center"
                         BorderBrush="{x:Null}"
                         Height="28"
                         VerticalContentAlignment="Center"
                         Foreground="#FF8F8F8F"
                         GotFocus="txtSearchMedID_GotFocus"
                         LostFocus="txtSearchMedID_LostFocus"
                         TextChanged="txtSearchMedID_TextChanged"
                         Text="Search medicine request ID..."/>
                                                    </Grid>
                                                </Border>

                                                <!-- ComboBox Filter -->
                                                <ComboBox x:Name="cboMedFilter"
                  Grid.Column="2"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top"
                  Width="170"
                  Style="{StaticResource CustomComboBoxStyle}"
                  SelectionChanged="cboMedFilter_SelectionChanged"
                  Height="30"
                  Foreground="Black"
                  Margin="10,26,0,0">
                                                    <ComboBoxItem Content="All"/>
                                                    <ComboBoxItem Content="Pending"/>
                                                    <ComboBoxItem Content="Approved"/>
                                                    <ComboBoxItem Content="Rejected"/>
                                                </ComboBox>
                                            </Grid>
                                        </Border>


                                    </Grid>
                                </Border>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="📅 Appointment" RenderTransformOrigin="0.5,0.5" Height="30" VerticalAlignment="Top"
         mah:HeaderedControlHelper.HeaderForeground="{x:Null}" BorderThickness="5,0,0,0"
         Background="{x:Null}" BorderBrush="{x:Null}" Margin="-155,-2,155,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                BorderBrush="#1A00104D"
                BorderThickness="1"
                CornerRadius="10,10,10,10"
                Margin="0,20,0,0" 
                Background="White">

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="70"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0" BorderBrush="#FFA0A0A0" BorderThickness="0,0,0,1" Margin="0,4,0,0">
                                        <Grid Height="65" VerticalAlignment="Bottom">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="270"/>
                                                <ColumnDefinition Width="240" MinWidth="221"/>
                                                <ColumnDefinition Width="1" MinWidth="180"/>
                                                <ColumnDefinition Width="1"/>
                                            </Grid.ColumnDefinitions>

                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="55.8" MinHeight="55.58"/>
                                            </Grid.RowDefinitions>

                                            <Label Content="Review and manage appointment requests"
                               Grid.Column="0"
                               Margin="10,30,0,0"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Top"
                               Foreground="#FFAFAFAF" Height="26" Width="236"/>

                                            <Border Grid.Column="1"
                                BorderBrush="#1A00104D"
                                BorderThickness="1"
                                CornerRadius="5"
                                VerticalAlignment="Top"
                                HorizontalAlignment="Center"
                                Width="220"
                                Height="32" Margin="0,24,0,0">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Image Grid.Column="0"
                                       HorizontalAlignment="Center"
                                       Height="20"
                                       VerticalAlignment="Center"
                                       Width="17"
                                       Margin="5,0,5,0"
                                       Source="/Screenshot 2025-10-21 210426.png"/>
                                                    <TextBox x:Name="txtSearchIDapp"
                                         Grid.Column="1"
                                         HorizontalAlignment="Stretch"
                                         TextWrapping="Wrap"
                                         VerticalAlignment="Center"
                                         TextChanged="txtSearchIDapp_TextChanged_1"
                                         BorderBrush="{x:Null}"
                                         Height="28" VerticalContentAlignment="Center" Foreground="#FF8F8F8F" GotFocus="txtSearchIDapp_GotFocus" LostFocus="txtSearchIDapp_LostFocus" Text="Search appointment ID..."/>
                                                </Grid>
                                            </Border>

                                            <ComboBox x:Name="cmbStatus"
                                  Grid.Column="2"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Top"
                                  Width="170"
                                  Style="{StaticResource CustomComboBoxStyle}"
                                  SelectionChanged="cmbStatus_SelectionChanged"
                                  Height="30" Foreground="Black" Margin="10,26,0,0">
                                                <ComboBoxItem Content="All"/>
                                                <ComboBoxItem Content="Pending"/>
                                                <ComboBoxItem Content="Approved"/>
                                                <ComboBoxItem Content="Rejected"/>
                                            </ComboBox>
                                            <Label Content="Appointments"
                               Foreground="#FF00104D"
                               FontSize="14" Padding="5,0,0,0" Margin="10,12,162,25" FontWeight="DemiBold"/>
                                        </Grid>
                                    </Border>

                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                        <StackPanel x:Name="AppointmentStackPanel" Orientation="Vertical" Margin="0"/>
                                    </ScrollViewer>

                                </Grid>
                            </Border>
                        </Grid>
                    </TabItem>


                    <TabItem Margin="19,-1,-49,-1" Header="👩‍🎓 Students" Background="{x:Null}" BorderBrush="{x:Null}">
                        <Grid>
                            <Border BorderBrush="#1A00104D" BorderThickness="1" Margin="0,26,0,0" CornerRadius="15,15,15,15" Background="White">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="31*"/>
                                        <RowDefinition Height="177*"/>
                                    </Grid.RowDefinitions>
                                    <Label Content="Student Management" HorizontalAlignment="Left" Margin="18,10,0,0" VerticalAlignment="Top" Width="224" Foreground="#FF00104D" FontSize="14"/>
                                    <Label Content="Manage student accounts and records" HorizontalAlignment="Left" Margin="18,31,0,0" VerticalAlignment="Top" Width="217" Foreground="#FFAFAFAF"/>
                                    <Image HorizontalAlignment="Center" Height="62" Margin="0,56,0,0" Grid.Row="1" VerticalAlignment="Top" Width="85" Source="/Screenshot 2025-10-22 124632.png"/>
                                    <Label Content="Comprehensive student account management system" HorizontalAlignment="Center" Margin="0,117,0,0" Grid.Row="1" VerticalAlignment="Top" Width="297"/>
                                    <Button Content="Manage Student"
        HorizontalAlignment="Center"
        Margin="0,151,0,0"
        Grid.Row="1"
        VerticalAlignment="Top"
        Width="162"
        Height="36" Cursor="Hand" Click="Button_Click_1"
                                                    Style="{StaticResource SelectedAppointmentButtonStyle}"
/>
                                    <Label Content="• Add new student accounts with comprehensive information&#xA;    • Edit existing student details and medical information&#xA;         • View enrollment analytics and program statistics&#xA;             • Search and filter students by various criteria" Grid.Row="1" Width="506" Foreground="#FFAFAFAF" HorizontalAlignment="Center" HorizontalContentAlignment="Center" VerticalAlignment="Top" Height="82" Margin="0,195,0,0"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </TabItem>
                    <TabItem Margin="65,-1,-96,0" Header="📦 Inventory">
                        <Grid Margin="0,0,5,61">
                            <Border BorderThickness="1"
        CornerRadius="10"
        Background="White"
        BorderBrush="#1A00104D"
        Margin="0,22,0,-64">
                                <Grid Margin="0,0,0,-1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" MinHeight="70"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>

                                    <!-- Header -->
                                    <Border Grid.Row="0"
                BorderBrush="#FF959595"
                BorderThickness="0,0,0,1"
                Margin="0,4,0,0"
                VerticalAlignment="Bottom">
                                        <Grid Height="65" VerticalAlignment="Bottom">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="270"/>
                                                <ColumnDefinition Width="240" MinWidth="221"/>
                                                <ColumnDefinition Width="181" MinWidth="180"/>
                                                <ColumnDefinition Width="0"/>
                                            </Grid.ColumnDefinitions>

                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="55.8" MinHeight="55.58"/>
                                            </Grid.RowDefinitions>

                                            <!-- Title -->
                                            <Label Content="Manage Inventory"
                       Foreground="#FF00104D"
                       FontSize="14"
                       FontWeight="DemiBold"
                       Padding="5,0,0,0"
                       Margin="10,12,0,25"/>

                                            <!-- Subtitle -->
                                            <Label Content="Manage and update medicine inventory"
                       Grid.Column="0"
                       Margin="10,30,0,0"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Foreground="#FFAFAFAF"
                       Height="26"
                       Width="236"/>

                                            <!-- Search Bar -->
                                            <Border Grid.Column="1"
                        BorderBrush="#1A00104D"
                        BorderThickness="1"
                        CornerRadius="5"
                        Height="32"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Center"
                        Margin="0,24,0,0"
                        Width="221"
                        Background="White">

                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Search Icon -->
                                                    <Image 
                            Source="/Screenshot 2025-10-21 210426.png"
                            Width="20" Height="20"
                            VerticalAlignment="Center"
                            Margin="10,0"/>

                                                    <!-- Search TextBox -->
                                                    <TextBox 
                            x:Name="txtSearch"
                            Grid.Column="1"
                            Margin="5,0,10,0"
                            VerticalAlignment="Center"
                            BorderThickness="0"
                            Background="Transparent"
                            Foreground="#FF8F8F8F"
                            FontSize="13"
                            Text="Search Inventory ID..."
                            TextChanged="txtSearch_TextChanged"
                            GotFocus="txtSearch_GotFocus"
                            LostFocus="txtSearch_LostFocus"/>
                                                </Grid>
                                            </Border>

                                            <!-- ComboBox Filter -->
                                            <ComboBox x:Name="cmbFilterInv"
                          Grid.Column="2"
                          VerticalAlignment="Top"
                          Width="171"
                          Height="30"
                          Margin="10,26,0,0"
                          Foreground="Black"
                          Style="{StaticResource CustomComboBoxStyle}"
                          SelectionChanged="ComboBox_SelectionChanged"
                          HorizontalAlignment="Left">
                                                <ComboBoxItem Content="Low Stacks"/>
                                                <ComboBoxItem Content="High Stacks"/>
                                                <ComboBoxItem Content="All Item"/>
                                            </ComboBox>
                                        </Grid>
                                    </Border>

                                    <!-- ScrollViewer content -->
                                    <ScrollViewer Grid.Row="1"
                      Margin="20"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
                                        <Grid HorizontalAlignment="Center">
                                            <WrapPanel x:Name="wrapPanelInventory"
                           Orientation="Horizontal"
                           Margin="10"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Grid>
                            </Border>




                        </Grid>
                    </TabItem>

                </TabControl>
                <Label Content="Admin Portal" HorizontalAlignment="Left" Margin="64,-56,0,0" VerticalAlignment="Top" FontSize="20" FontWeight="Bold"/>
            </Grid>
            <mah:HamburgerMenu Content="HamburgerMenu" HorizontalAlignment="Left" Margin="327,-63,0,0" VerticalAlignment="Top" Height="1" RenderTransformOrigin="0.5,0.5" Width="6">
                <mah:HamburgerMenu.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="-1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </mah:HamburgerMenu.RenderTransform>
            </mah:HamburgerMenu>
        </Grid>
    </Border>
</Window>